---
---
<section id="contact" class="py-24 px-6 bg-navy relative overflow-hidden">
  <!-- Background Elements -->
  <div class="absolute top-0 left-0 w-full h-px bg-gradient-to-r from-transparent via-teal/20 to-transparent"></div>
  <div class="absolute bottom-0 left-0 w-full h-px bg-gradient-to-r from-transparent via-teal/20 to-transparent"></div>
  
  <div class="max-w-4xl mx-auto">
    <div class="text-center mb-16 space-y-4">
      <h2 class="text-xs font-mono text-teal tracking-widest uppercase">Get in Touch</h2>
      <h3 class="text-4xl md:text-5xl text-white font-bold">Start a Conversation</h3>
      <p class="text-slate text-lg max-w-2xl mx-auto">
        Whether you're looking for collaboration on 5G/RAN projects, consulting services, or just want to connect.
      </p>
    </div>

    <form 
      name="contact" 
      method="POST" 
      data-netlify="true" 
      netlify-honeypot="bot-field" 
      action="/success"
      class="bg-[#020617] border border-white/5 rounded-2xl p-8 md:p-12 shadow-2xl space-y-8 relative"
    >
      <!-- Hidden input for Netlify -->
      <input type="hidden" name="form-name" value="contact" />
      <p class="hidden" style="display:none">
        <label>Don’t fill this out if you’re human: <input name="bot-field" /></label>
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- Full Name -->
        <div class="space-y-2">
          <label for="name" class="block text-sm font-mono text-slate-300">Full Name <span class="text-teal">*</span></label>
          <input 
            type="text" 
            id="name" 
            name="name" 
            required
            class="w-full bg-navy/50 border border-slate/30 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-teal focus:ring-1 focus:ring-teal transition-all placeholder-slate/30"
            placeholder="John Doe"
          />
        </div>

        <!-- Professional Email -->
        <div class="space-y-2">
          <label for="email" class="block text-sm font-mono text-slate-300">Professional Email <span class="text-teal">*</span></label>
          <input 
            type="email" 
            id="email" 
            name="email" 
            required
            class="w-full bg-navy/50 border border-slate/30 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-teal focus:ring-1 focus:ring-teal transition-all placeholder-slate/30"
            placeholder="john@company.com"
          />
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- Company / Organization -->
        <div class="space-y-2">
          <label for="company" class="block text-sm font-mono text-slate-300">Company / Organization</label>
          <input 
            type="text" 
            id="company" 
            name="company" 
            class="w-full bg-navy/50 border border-slate/30 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-teal focus:ring-1 focus:ring-teal transition-all placeholder-slate/30"
            placeholder="Tech Corp Ltd."
          />
        </div>

        <!-- Inquiry Type -->
        <div class="space-y-2">
          <label for="inquiry-type" class="block text-sm font-mono text-slate-300">Inquiry Type</label>
          <div class="relative">
            <select 
              id="inquiry-type" 
              name="inquiry-type"
              class="w-full bg-navy/50 border border-slate/30 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-teal focus:ring-1 focus:ring-teal transition-all appearance-none cursor-pointer"
            >
              <option value="Request CV">Request CV</option>
              <option value="Project Collaboration">Project Collaboration</option>
              <option value="Consulting Inquiry">Consulting Inquiry</option>
            </select>
            <div class="absolute inset-y-0 right-0 flex items-center px-4 pointer-events-none text-slate-400">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
            </div>
          </div>
        </div>
      </div>

      <!-- Message -->
      <div class="space-y-2">
        <label for="message" class="block text-sm font-mono text-slate-300">Message</label>
        <textarea 
          id="message" 
          name="message" 
          rows="4"
          class="w-full bg-navy/50 border border-slate/30 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-teal focus:ring-1 focus:ring-teal transition-all placeholder-slate/30 resize-none"
          placeholder="How can we work together?"
        ></textarea>
      </div>

      <!-- Submit Button -->
      <div class="pt-4">
        <button 
          type="submit"
          class="w-full md:w-auto px-8 py-4 bg-teal text-navy font-bold rounded-lg hover:bg-[#2DD4BF] hover:shadow-[0_0_20px_rgba(20,184,166,0.4)] transition-all duration-300 flex items-center justify-center group"
        >
          Send Message
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </button>
      </div>
    </form>
